# ===================================
# INTOWORK - Frontend Production Environment (Vercel)
# ===================================

# NextAuth Configuration
# Must match NEXTAUTH_SECRET in backend
NEXTAUTH_URL=https://intowork.co
NEXTAUTH_SECRET=your-production-nextauth-secret-min-32-characters-same-as-backend
AUTH_SECRET=your-production-nextauth-secret-min-32-characters-same-as-backend

# Backend API URL (MUST use HTTPS in production, MUST include /api suffix)
# Railway backend URL (replace with your actual Railway deployment URL)
NEXT_PUBLIC_API_URL=https://intowork-dashboard-production-1ede.up.railway.app/api

# Environment
NODE_ENV=production

# IMPORTANT NOTES:
# 1. NEXTAUTH_SECRET MUST match the backend's NEXTAUTH_SECRET exactly
# 2. NEXT_PUBLIC_API_URL MUST use HTTPS (browser blocks HTTP on HTTPS sites)
# 3. NEXT_PUBLIC_API_URL MUST end with /api (backend routes use this prefix)
# 4. These variables should be set in Vercel dashboard for production deployment
